---
# tasks file for build
- name: sync war
  synchronize:
    mode: push
    src: '/home/avasekho/hwk9_docker/build/files/Dockerfile'
    dest: '/tmp/maven/Dockerfile'

- name: Build maven container
  docker_compose:
    project_name: maven
    definition:
      version: '2'
      services:
        maven:
          build: "/tmp/maven"
          volumes:
            - "/tmp/shared_folder:/shared_folder:rw"