---
# tasks file for build
- name: Build maven container
  docker_compose:
    project_name: maven
    definition:
      version: '2'
      services:
        maven:
          build: "/home/avasekho/hwk9_docker/build/files/"
          volumes:
            - "/tmp/shared_folder:/shared_folder:rw"